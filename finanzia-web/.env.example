# FinanZIA Web — Variables de Entorno
# Copia este archivo a .env.local para desarrollo local

# URL pública del backend API (para llamadas desde el cliente)
NEXT_PUBLIC_API_URL=http://localhost:3001

# URL interna del backend (usada por Next.js rewrite / Docker)
INTERNAL_API_URL=http://localhost:3001
